{
    "createStatements": [
        "DROP TABLE IF EXISTS accounts;",
        "CREATE TABLE accounts(id INTEGER PRIMARY KEY, name TEXT NOT NULL, opening_balance INTEGER NOT NULL, currency_id INTEGER NOT NULL, closed_flag INTEGER NOT NULL);"
    ]
}