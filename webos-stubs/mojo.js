/**
 * The file contains stubs for some webos classes/functions in order to
 * "fix" IDE inspections warnings about non-exiting methods or classes.
 *
 * Note: the stubs don't included into application package and should be
 * used for purpose of IDE webos support improvement.
 */

Mojo.require = function(s,t,u,b) {
};

Mojo.requireEqual = function(s,t,u,b) {
};

Mojo.requireFalse = function(s,t,u,b) {
};

Mojo.requireFunction = function(s,t,u,b) {
};

Mojo.requireNumber = function(s,t,u,b) {
};

Mojo.requireString = function(s,t,u,b) {
};